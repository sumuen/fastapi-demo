#!/usr/bin/env python3
# -*- coding: utf-8 -*-
from fastapi import APIRouter

# from backend.app.demo.api.product import router as product_router
# from backend.app.demo.api.order import router as order_router

router = APIRouter()

# router.include_router(product_router, prefix="/products", tags=["Products"])
# router.include_router(order_router, prefix="/orders", tags=["Orders"]) 